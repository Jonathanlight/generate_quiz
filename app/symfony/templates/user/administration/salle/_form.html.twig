{{ form_start(form) }}
<div class="row">
    <div class="col-md-12">
        <div class="mb-3">
            {{ form_row(form.name, {
                label: 'administration.salle.form.name.name',
                attr: {
                    'class': 'form-control',
                    'placeholder': 'administration.salle.form.name.placeholder'
                }
            }) }}
        </div>

        <div class="mb-3">
            {{ form_row(form.center, {
                label: 'administration.salle.form.center.name',
                attr: {
                    'class': 'form-select',
                    'placeholder': 'administration.salle.form.center.placeholder'
                }
            }) }}
        </div>

        <div class="mb-3">
            {{ form_row(form.formation, {
                label: 'administration.salle.form.formation.name',
                attr: {
                    'class': 'form-select',
                    'placeholder': 'administration.salle.form.formation.placeholder'
                }
            }) }}
        </div>

        <div class="mb-3">
            {{ form_row(form.status, {
                label: 'administration.salle.form.status.name',
                attr: {
                    'class': 'form-select',
                    'placeholder': 'administration.salle.form.status.placeholder'
                }
            }) }}
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="mb-3">
            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
        </div>
    </div>
</div>
{{ form_end(form) }}

