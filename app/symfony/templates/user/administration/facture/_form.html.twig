{{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">
            <div class="mb-3">
                {{ form_row(form.total, {
                    label: 'administration.facture.form.total.name',
                    attr: {
                        'class': 'form-control',
                        'placeholder': 'administration.facture.form.total.placeholder'
                    }
                }) }}
            </div>
        </div>
        <div class="col-md-6">
            <div class="mb-3">
                {{ form_row(form.salle, {
                    label: 'administration.facture.form.salle.name',
                    attr: {
                        'class': 'form-select',
                        'placeholder': 'administration.facture.form.salle.placeholder'
                    }
                }) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
            </div>
        </div>
    </div>
{{ form_end(form) }}