<form method="post" action="{{ path('site_formation_delete', {'id': site_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site_formation.id) }}">
    <button class="btn btn-outline-danger mt-2 mb-2">Supprimer</button>
</form>
